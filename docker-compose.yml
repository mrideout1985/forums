services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: forum
      POSTGRES_USER: forum
      POSTGRES_PASSWORD: forum
    ports:
      - "5432:5432"
    volumes:
      - forum_pg:/var/lib/postgresql/data

volumes:
  forum_pg:
